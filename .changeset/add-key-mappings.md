---
"obsidian-terminal": minor
---

Add custom key mapping system for iTerm2-style key remapping. Users can define key combos and what the terminal sends when pressed (ignore, escape sequence, hex code, or text). Also fixes a bug where two `attachCustomKeyEventHandler` calls silently overwrote each other.
