---
"obsidian-terminal": minor
---

Add a per-profile setting to define environment variables for external and integrated terminal profiles. Each entry is a `KEY=value` pair and is merged into the spawned shell's environment. ([GH#161](https://github.com/polyipseity/obsidian-terminal/pull/161) by [@SAY-5](https://github.com/SAY-5))
