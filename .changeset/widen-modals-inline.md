---
"obsidian-terminal": patch
---

Widen modals to ensure they are usable on all themes by disabling `dynamicWidth`. This makes the profile editor and list modals have a width that can be set by theme CSS.
